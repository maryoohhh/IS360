
USE TSQL2018;
GO

  -- Query the HR.Employees to return the titleofcourtesy column with the alias 'Title',
  -- the firstname, lastname, and mgrid columns.
  

  -- Modify the query to filter out rows with mgrid NULL 


 -- Modify the query to filter out rows with mgrid non-NULL
  

 -- Create a query to return all rows from HR.Employees. Use COALESCE( ) to replace NULLs in the mgrid column with zero


 -- Bonus: Return all rows from HR.Employees. Use a CASE expression to replace the NULLs in the mgrid column with zero 
 
 -- Bonus X 2: Return all rows from HR.Employees. Use a CASE expression to replace the NULLs in the mgrid column with 'CEO'